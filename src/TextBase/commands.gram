#JSGF V1.0;

grammar commands;

<commands> = ( 
                root
| male
| female
| downloads
| trance
| books
| dragon ball z episode one
);


























public <command> = <commands>;
